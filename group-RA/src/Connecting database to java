# JDBC MySQL Connection Guide (VS Code)

## 🎯 Objective

Connect a Java program to a MySQL database using JDBC in VS Code.

---

## 🧱 Prerequisites

Make sure you have:

1. Java JDK installed
2. VS Code installed
3. MySQL Server running
4. Database created (example: `demo`)

---

## 📁 Step 1: Create Project Structure

Create a folder:

```
jdbc-demo/
 ├── lib/
 ├── src/
 │    └── DatabaseConnection.java
```

---

## 📦 Step 2: Add MySQL JDBC Driver

1. Download MySQL Connector/J (ZIP)
2. Extract it
3. Copy `.jar` file into `lib/`

Example:

```
lib/mysql-connector-j-9.7.0.jar
```

---

## ⚙️ Step 3: Configure VS Code

Create a file:

```
.vscode/settings.json
```

Add:

```json
{
  "java.project.referencedLibraries": [
    "lib/**/*.jar"
  ]
}
```

👉 This tells VS Code to include the driver.

---

## 💻 Step 4: Write Java Code

```java
import java.sql.Connection;
import java.sql.DriverManager;

public class DatabaseConnection {
    public static void main(String[] args) {

        String url = "jdbc:mysql://localhost:3306/demo";
        String user = "root";
        String password = "root123";

        try {
            Class.forName("com.mysql.cj.jdbc.Driver");

            Connection conn = DriverManager.getConnection(url, user, password);

            System.out.println("Connected to the database!");

            conn.close();

        } catch (Exception e) {
            e.printStackTrace();
        }
    }
}
```

---

## ▶️ Step 5: Run the Program

In VS Code:

* Open the file
* Click **Run** button
  OR use terminal:

```
javac -cp "lib/*" src/DatabaseConnection.java
java -cp "lib/*;src" DatabaseConnection
```

---

## ✅ Expected Output

```
Connected to the database!
```

---

## ⚠️ Common Errors & Fixes

### ❌ No suitable driver

👉 Driver not added → check `lib/` and settings.json

---

### ❌ Access denied

👉 Wrong username/password

---

### ❌ Connection refused

👉 MySQL server not running

---

## 🧠 Key Concept

```
JDBC Flow:
Java → Driver → Database
```

---

## 🚀 Quick Recap

1. Add driver (.jar)
2. Configure VS Code
3. Write connection code
4. Run program

---

## 📌 Tip for Students

Use simple credentials:

```
username: root
password: root123
database: demo
```

---

## 🎯 Next Step

After connection works, move to:

* INSERT
* SELECT
* UPDATE
* DELETE

---
